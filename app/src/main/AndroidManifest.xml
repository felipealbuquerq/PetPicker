<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.dinosilvestro.petpicker">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <service
            android:name=".model.FetchAddressIntentService"
            android:exported="false"/>

        <activity
            android:name=".view.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".view.ShelterListActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".view.ShelterDetailActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".view.PetGridActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".view.PetDetailActivity"
            android:screenOrientation="portrait"/>

        <provider
            android:name=".model.PetProvider"
            android:authorities="com.dinosilvestro.petpicker"
            android:exported="false"
            android:syncable="true"/>

        <activity
            android:name=".view.PetPicksGridActivity"
            android:screenOrientation="portrait"/>

        <receiver
            android:name=".widget.WidgetProvider"
            android:label="Pet Picker">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info"/>
        </receiver>

        <service
            android:name=".widget.WidgetRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS"/>

    </application>

</manifest>